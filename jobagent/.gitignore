# Secrets and per-user state — never commit these
.env
profile/vault.enc
profile/.vault.key
browser_profile*/
output/
*.db

# Personal data — NEVER commit your filled-in versions. Copy the *.example
# files to these names and edit locally; the real ones stay off git.
profile/master_resume.md
profile/answers.yaml
profile/*.db

__pycache__/
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/
