# Secrets and per-user state — never commit these
.env
profile/vault.enc
profile/.vault.key
browser_profile*/
output/
*.db

# Personal data: your filled-in resume/answers stay local — only the
# .example templates are committed.
profile/master_resume.md
profile/answers.yaml

__pycache__/
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/
